# 更新日志

## 1.3.0

- 新增：Planner 可直接调用 `linshe_generate_image` 工具触发邻舍生图，工具只提交画面需求，邻舍按发图助手固定格式（short_prompt + 外观段）提炼画面需求为 prompt 并应用角色 LoRA，异步后台取图发送，不阻塞继续说话。
- 新增：邻舍侧新增 `POST /api/maibot/generate` 专用生图接口，原 `/api/maibot/chat` 等接口保留兼容。
- 变更：回复后的自动配图判断改为只同步记忆，配图统一由 Planner 工具触发。
- 变更：生图工具说明改为桥接注入时临时追加到行为风格（写明可调用 `linshe_generate_image`），不再保存进人物行为风格或在前端显示；同时清理旧版 base_prompt 里的遗留句子。
- 新增：插件配置新增「永久写入MaiBot配置代替临时覆盖」，勾选后会把邻舍人物卡（人格/行为/表达风格/机器人昵称）直接写入 MaiBot 配置，MaiBot 单独启动也能使用。
- 修复：`linshe_generate_image` 同一句用户消息只允许触发一次，且同聊天流同时只允许一个生图任务，避免一条消息重复触发多张图片。
- 修复：工具说明与返回明确禁止引用工具结果媒体，避免 `reply` 因无效 `media_index` 失败导致文字丢失。
- 修复：生图工具调用说明只注入 Planner，不再注入 replyer，避免回复文本把 `<function_calls>` 当正文发出去。
- 修复：生图需求的历史聊天记录最多保留 4 条，assistant 消息继续统一标记为“你”。
- 变更：移除插件配置 `image_mode` 与 `context_max_messages`，保留生图轮询间隔/超时。

## 1.2.5

- 新增：插件设置页的「邻舍角色显示名」使用当前激活角色的 `display_name`，无本地显示名时回退为配置中的角色名。
- 变更：移除 WebUI 首页 HomeCard，人格管理页入口改到插件设置页。
- 新增：表达风格在提炼、读取与保存时都会在末尾确保追加「**回复限制在30字内**」，已包含该限制时不重复追加。
- 新增：表达风格末尾追加「**禁止发送Unicode文本类型的emoji**」，已包含该限制时不重复追加。
- 新增：邻舍侧 `/api/maibot/characters` 与 `/api/maibot/derive-style` 返回时统一追加画图能力说明与回复字数限制，新角色首次选中即可在管理页看到。
- 新增：配图发送按聊天流（群）限流，同一群 30 秒内最多发送一张；冷却期内跳过邻舍配图判断，记忆仍正常同步。
- 新增：注入前在 base_prompt 末尾追加画图能力说明「## 你拥有画图的能力，只要你想象画面，你就可以发送出来图片」，并写回本地人格存档，管理页可见。

## 1.2.4

- 新增：将 MaiBot 请求文本中的机器人昵称替换为邻舍角色的 `display_name`，Planner 与 replyer 均生效。

## 1.2.3

- 修复：新增 `maisaka.planner.before_request` 注入，Planner 不再沿用 MaiBot 默认行为风格。
- 修复：replyer 注入时移除 MaiBot 自带的表达习惯与一次性回复风格，邻舍表达风格完全接管。

## 1.2.2

- 修复：首页人格管理卡片链接改为跟随 `bridge.base_url`，不再硬编码 `127.0.0.1:3099`。
- 修复：图片下载兼容绝对 URL，邻舍返回完整地址时不再错误拼接 `base_url`。
- 变更：`host_application.max_version` 更新至 `1.1.4`。

## 1.2.1

- 变更：人格数据目录固定使用 MaiBot 插件默认目录，管理页与插件运行时不再存在路径分叉。
- 变更：移除邻舍管理页中的“插件数据目录”配置及 `maibot_plugin_data_dir` 设置读写。
- 变更：人格数据改由插件公开 API 读写，并写入 SDK 分配的标准持久化目录；邻舍侧不再直接访问 MaiBot 文件系统。
- 变更：移除尚未发布版本中的旧人格数据迁移兼容逻辑。

## 1.2.0

- 变更：行为/表达风格与 base_prompt 本地副本改为按角色保存在插件数据目录 persona_store.json（不再写入邻舍数据库）。
- 变更：移除全局覆盖字段（persona_base_prompt_override / behavior_style_override / reply_style_override），覆盖入口统一为插件本地数据（人格管理页或直接编辑数据文件）。
- 新增：首次注入时保存邻舍 base_prompt 到本地副本，用户可修改后固定人格原文；行为/表达风格提炼后自动保存，已保存时不再重复提炼。
- 变更：移除过时的 style_cache_ttl_sec / style_refresh_tick 配置（风格改为按角色持久化到本地数据，解析时直接读取，无需缓存时长与刷新标记）。

## 1.1.0

- 新增：人格原文覆盖字段 persona_base_prompt_override，留空使用邻舍角色 base_prompt，填写后整条替换 system 人格。
- 新增：行为/表达风格覆盖字段 behavior_style_override、reply_style_override，留空使用邻舍提炼结果，填写后注入时优先使用覆盖值。
- 新增：提炼出的行为/表达风格按角色保存到邻舍侧（人格管理页可见可覆盖），插件本地保留离线缓存，日后可在配置或管理页覆盖。

## 1.0.0

- 新增：人格注入，将 system 人格整条替换为邻舍角色的 base_prompt。
- 新增：调用邻舍 derive-style 提炼行为风格与表达风格并注入模型请求。
- 新增：回复生成后将本轮对话写入邻舍记忆库（不动情绪/好感度）。
- 新增：由邻舍判断是否配图，异步轮询生图任务并发送图片。
